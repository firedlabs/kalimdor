import styled from 'styled-components'
import { Section } from '@firedlabs/design-system'

export const SponsorStyles = styled(Section).attrs({
  backgroundColor: 'colorSuccess',
  borderColor: 'colorSecond'
})`
  display: flex;
  flex-direction: column;
  align-items: center;
`
