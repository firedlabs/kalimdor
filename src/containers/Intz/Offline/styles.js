import styled from 'styled-components'
import * as I from 'intz-design-system'

export const Wrapper = styled.section`
  display: flex;
  align-items: center;
  flex-wrap: wrap;
`

export const Avatar = styled(I.Avatar)`
  width: 70px;
  margin: 0 var(--gap-small) var(--gap-small);
`
